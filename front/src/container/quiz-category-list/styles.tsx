import styled from "styled-components/macro";

import { THEME_COLOR } from "../../constant";

export const QuizCategoryList = styled.ul`
  margin: 0;
  padding: 0;
  border-bottom: 1px solid ${THEME_COLOR.PRIMARY};
`;
